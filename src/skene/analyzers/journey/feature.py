"""Feature type emitted by the schema and code agents.

The model itself lives in :mod:`skene.schema.feature` (it is the typed
payload of ``FeaturePart`` on the wire); this module is the engine-side
import home.
"""

from __future__ import annotations

from skene.schema.feature import Feature as Feature
